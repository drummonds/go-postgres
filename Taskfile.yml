version: "3"

tasks:
  test:
    desc: Run all tests
    cmds:
      - go test -count=1 ./...

  build:
    desc: Build all packages
    cmds:
      - go build ./...


  check:
    desc: Release steps
    cmds:
      - task test
      - task build


steps:
  - name: soak-test
    image: golang:1.26
    environment:
      SOAK_DURATION: 2m
      SOAK_WORKERS: 4
      SOAK_METRIC_INTERVAL: 10s
    commands:
      - go test -run TestSoak -timeout 5m -count=1 -v

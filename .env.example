# Copy to ~/.config/go-postgres.env (shared across worktrees)
# or to .env in the project root (per-worktree override)

# Hetzner Cloud SSH key name (from: hcloud ssh-key list)
HCLOUD_SSH_KEY=
# Hetzner Cloud API token (from: console.hetzner.cloud → Security → API Tokens)
HCLOUD_TOKEN=
